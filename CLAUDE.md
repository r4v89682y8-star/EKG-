# أصول — قيود المشروع الدائمة

تطبيق إدارة أملاك عربي (RTL) في ملف واحد: `index.html`. القواعد التالية ملزِمة لأي تعديل.

## ١ — ملف واحد لا يُقسَّم

`index.html` هو التطبيق كله: Tailwind من CDN، و`@supabase/supabase-js@2` من unpkg، والأنماط والشيفرة مضمّنة فيه.

- لا تُقسّم الملف إلى ملفات CSS أو JS منفصلة.
- لا تُضف أدوات بناء: لا bundler، ولا npm للتطبيق نفسه، ولا إطار عمل، ولا خطوة ترجمة.
- التعديلات تكون في مكانها، بأسلوب الملف القائم: أصناف Tailwind، ومعالِجات `onclick` مضمّنة، وعرض عبر إسناد قوالب نصية إلى `innerHTML`.

استثناء وحيد: `scripts/` — أدوات فحص تعمل بـ Node خارج التطبيق، ولا يُحمّلها المتصفّح ولا تدخل في بنائه.

## ٢ — الأساس المحاسبي

لا يُحسب أي مبلغ إلا عبر الدوال الثلاث:

- `dueOf(p)` — كامل المستحق على الدفعة (الإيجار + الضريبة).
- `paidOf(p)` — ما قُبض فعلًا منها، ويشمل السداد الجزئي.
- `restOf(p)` — المتبقّي عليها.

ومجاميعها: `sumDue` و`sumPaid` و`sumRest`، وفحص السداد بـ `isSettled(p)`.

ممنوع قراءة `p.amount` أو `p.paid_amount` أو `p.vat_amount` مباشرةً في أي حساب أو عرض. الوصول المباشر مسموح فقط داخل تعريف الدوال الثلاث نفسها، وعند بناء صفوف جديدة للإدراج في قاعدة البيانات.

**السبب:** السداد الجزئي والضريبة يجعلان «المبلغ» أكثر من حقل واحد؛ أي حساب يتجاوز هذه الدوال يعطي رقمًا مختلفًا عن بقية الشاشات.

## ٣ — تهريب بيانات المستخدم

كل نص مصدره قاعدة البيانات أو إدخال المستخدم يُهرَّب قبل عرضه:

- `esc(v)` — للعرض داخل HTML.
- `escJs(v)` — لتمرير قيمة داخل سمة `onclick` أو ما شابهها. تُهرّب سلسلة JS أولًا (الشرطة المائلة قبل غيرها) ثم HTML، لأن المتصفّح يفكّ ترميز السمة قبل قراءة الشيفرة.

المعرّفات (`id`) الآتية من Supabase لا تحتاج تهريبًا، لكن الأسماء والأوصاف والملاحظات وأرقام الوحدات تحتاجه دائمًا.

## ٤ — حراسة دوال الرسم

في أعلى السكربت اعتراض على `document.getElementById`: إن غاب العنصر يعود عنصر صوري يحمل `dataset.ghost` بدل `null`، حتى لا يسقط التنفيذ.

لذلك كل دالة تكتب في عنصر تجلبه بنفسها تبدأ بالحراسة:

```js
const box = document.getElementById('...'); if(box.dataset.ghost) return;
```

**السبب:** نقاط الرسم تُستدعى أحيانًا بعد إعادة بناء الصفحة، فيكون العنصر قد اختفى. بلا حراسة تكتب الدالة في العدم وتُخفي الخلل.

## ٥ — لغة الواجهة

كل ما يراه المستخدم بالعربية: العناوين، والأزرار، ورسائل الخطأ، ونصوص `confirm` و`alert`، والنصوص النائبة. الإنجليزية تأتي فقط من قاموس الترجمة `DICT` ومحرّكه، لا من نصوص مكتوبة مباشرة.

## ٦ — رقم الإصدار

ارفع `VERSION` (أعلى الملف) بعد كل تعديل: `v30` ← `v31` ← `v32`.

**السبب:** الملف يُنشر كما هو، ويتأكد المستخدم من وصول النسخة الجديدة بضغطة مطوّلة على الشعار تعرض الرقم.

## ٧ — لا حذف بلا طلب

لا تحذف دالة قائمة ولا شاشة ولا حقلًا إلا بطلب صريح. عند تغيير سلوك دالة، عدّلها في مكانها بدل استبدالها بأخرى.

## الفحص الآلي

```
node scripts/check.js
```

يفحص صحة الصياغة، ومعالِجات `onclick`/`onchange` هل دوالها معرّفة، والكتابة في `innerHTML` بلا حراسة، والحسابات المالية التي تتجاوز الأساس المحاسبي، والبيانات المعروضة بلا تهريب. شغّله بعد كل تعديل قبل النشر.
